{
  "name": "@positron/examples",
  "version": "0.0.0",
  "description": "Examples for the Positron 3D game engine.",
  "main": "index.js",
  "scripts": {
    "clean": "del dist",
    "prebuild": "npm run clean",
    "build": "run-p build:assets build:pug build:ts",
    "build:assets": "cpx node_modules/@positron/core/dist/positronjs dist/assets",
    "build:pug": "pug src --out ./dist --pretty",
    "build:ts": "rollup -c",
    "prestart": "npm run clean",
    "start": "run-p watch start:server",
    "start:server": "http-server ./dist",
    "watch": "run-p watch:assets watch:pug watch:ts",
    "watch:assets": "cpx -w node_modules/@positron/core/dist/positron.js dist/assets",
    "watch:pug": "pug src --out ./dist --pretty --watch",
    "watch:ts": "rollup -cw"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@positron/core": "0.0.0"
  },
  "devDependencies": {
    "cpx": "^1.5.0",
    "del-cli": "^1.1.0",
    "http-server": "^0.11.1",
    "npm-run-all": "^4.1.5",
    "pug": "^2.0.3",
    "pug-cli": "^1.0.0-alpha6",
    "rollup": "^1.1.0",
    "rollup-plugin-multi-entry": "^2.1.0",
    "rollup-plugin-typescript": "^1.0.0",
    "rollup-plugin-typescript2": "^0.18.1",
    "typescript": "^3.2.2"
  }
}
